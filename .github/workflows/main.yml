name: generate_calendar

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch: # Allow manual triggering

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Generate calendar files
        env:
          MLS_SEASONS: ${{ vars.MLS_SEASONS }}
          MLS_LEAGUE: ${{ vars.MLS_LEAGUE }}
          OUTPUT_ROOT: ${{ github.workspace }}
          MLS_NUM_TEAMS_EXPECTED: ${{ vars.MLS_NUM_TEAMS_EXPECTED }}
          MLS_LEAGUE_COMPETITIONS: ${{ vars.MLS_LEAGUE_COMPETITIONS }}
          MLS_INTERNATIONAL_COMPETITIONS: ${{ vars.MLS_INTERNATIONAL_COMPETITIONS }}
        run: |-
          cd $GITHUB_WORKSPACE
          python -m src.main
      - name: Commit calendar files if changed
        run: |-
          git diff
          git config --global user.email "actions@users.noreply.github.com"
          git config --global user.name "JEFF-bot"
          git add calendars/
          if git commit -m "Updated calendars"; then
            TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
            sed -i "s/lastUpdated = '.*'/lastUpdated = '$TIMESTAMP'/" client/src/app/app.component.ts
            git add client/src/app/app.component.ts
            git commit --amend --no-edit
            git push
          else
            echo "No changes to commit"
          fi
