name: generate_calendar

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch: # Allow manual triggering

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Generate calendar files
        env:
          SEASONS: ${{ vars.SEASONS }}
          LEAGUE: ${{ vars.LEAGUE }}
          OUTPUT_ROOT: ${{ github.workspace }}
          NUM_TEAMS_EXPECTED: ${{ vars.NUM_TEAMS_EXPECTED }}
          # IMPORTANT: This variable was renamed from EXCLUDED_COMPETITIONS to MLS_EXCLUDED_COMPETITIONS.
          # If the GitHub Actions repository variable has not been renamed in the repo settings,
          # MLS_EXCLUDED_COMPETITIONS will be empty and non-MLS competitions will silently flood
          # the league calendar. Rename the variable in Settings → Variables → Actions before merging.
          MLS_EXCLUDED_COMPETITIONS: ${{ vars.MLS_EXCLUDED_COMPETITIONS }}
          TOURNAMENT_COMPETITIONS: ${{ vars.TOURNAMENT_COMPETITIONS }}
        run: |-
          cd $GITHUB_WORKSPACE
          python -m src.main
      - name: Commit calendar files if changed
        run: |-
          git diff
          git config --global user.email "actions@users.noreply.github.com"
          git config --global user.name "JEFF-bot"
          git add calendars/
          if git commit -m "Updated calendars"; then
            TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
            sed -i "s/lastUpdated = '.*'/lastUpdated = '$TIMESTAMP'/" client/src/app/app.component.ts
            git add client/src/app/app.component.ts
            git commit --amend --no-edit
            git push
          else
            echo "No changes to commit"
          fi
